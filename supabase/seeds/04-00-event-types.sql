INSERT INTO event_types (tree_id, id, name)
VALUES
    /* *************************************************************************
     * HARRY POTTER
     * ************************************************************************* */
    
    (get_tree_id('hp'), get_event_type_id('birth'),  'Birth'),
    (get_tree_id('hp'), get_event_type_id('death'), 'Death'),
    (get_tree_id('hp'), get_event_type_id('marriage'), 'Marriage'),
    (get_tree_id('hp'), get_event_type_id('baptism'), 'Baptism'),
    (get_tree_id('hp'), get_event_type_id('burial'), 'Burial'),
    (get_tree_id('hp'), get_event_type_id('immigration'), 'Immigration'),
    (get_tree_id('hp'), get_event_type_id('census'), 'Census'),
    (get_tree_id('hp'), get_event_type_id('engagement'), 'Engagement'),
    (get_tree_id('hp'), get_event_type_id('separation'), 'Separation'),
    (get_tree_id('hp'), get_event_type_id('retirement'), 'Retirement'),
    (get_tree_id('hp'), get_event_type_id('other'), 'Other');

    /* *************************************************************************
     * GAME OF THRONES
     * ************************************************************************* */
    -- TODO: Add Game of Thrones event types

    /* *************************************************************************
     * NOS ÉTÉS
     * ************************************************************************* */
    -- TODO: Add Nos Étés event types 
