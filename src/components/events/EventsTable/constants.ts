import type { SortingConfig } from "./types";

export const DEFAULT_ADD_BUTTON_PATH = "/events/new";
export const DEFAULT_SEARCH_PLACEHOLDER = "Search events";
export const DEFAULT_SORTING: SortingConfig = { id: "date", desc: false };
