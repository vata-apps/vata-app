import type { SortingConfig } from "./types";

export const DEFAULT_ADD_BUTTON_PATH = "/individuals/new";
export const DEFAULT_SEARCH_PLACEHOLDER = "Search by name";
export const DEFAULT_SORTING: SortingConfig = { id: "last_name", desc: false };
