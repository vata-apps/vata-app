---
name: typescript-standards
description: Ensures React/TypeScript code follows project conventions. Use when writing or reviewing src/**/*.{ts,tsx} files (excluding src/db/** and src/lib/gedcom/**) — components, hooks, managers, store, routes, and types.
---

# TypeScript & React Standards

Apply this skill when writing or reviewing React/TypeScript code in the project. The app uses React 18, TypeScript 5 (strict mode), TanStack Query v5, TanStack Router v1, and Zustand v4.

## When to Apply

- Writing or reviewing `src/components/**`, `src/pages/**`, `src/routes/**`
- Writing or reviewing `src/managers/**`, `src/hooks/**`
- Writing or reviewing `src/store/**`, `src/lib/**`, `src/types/**`

---

## 1. TypeScript Rules

- **No `any`**: Use `unknown` for dynamic values, then narrow with type guards.
- **Prefer `interface`** for object shapes that may be extended; use `type` for unions, intersections, and aliases.
- **`strict: true`** is enabled: `noImplicitAny`, `strictNullChecks`, `noUnusedLocals`, `noUnusedParameters` are all enforced.
- **Return types**: Always annotate return types on exported functions.
- **Generics**: Use generics for reusable utilities; avoid `as` casts.
- **Path aliases**: Use project aliases instead of relative paths:

| Alias           | Resolves to        |
| --------------- | ------------------ |
| `$/*`           | `src/*`            |
| `$lib/*`        | `src/lib/*`        |
| `$components/*` | `src/components/*` |
| `$hooks/*`      | `src/hooks/*`      |
| `$managers`     | `src/managers`     |
| `$db`           | `src/db`           |
| `$db-system/*`  | `src/db/system/*`  |
| `$db-tree/*`    | `src/db/trees/*`   |
| `$types`        | `src/types`        |

---

## 2. TanStack Query Patterns

- **Query keys**: Always use the `queryKeys` factory from `src/lib/query-keys.ts`. Never hardcode query key arrays inline.
- **`staleTime`**: Set globally in `src/lib/query-client.ts` (5 min). Do not override per-query unless there is a documented reason.
- **Mutations**: Always call `queryClient.invalidateQueries` in `onSuccess` to keep the cache fresh.
- **Error handling**: Handle `isError` states in UI — never silently swallow query errors.
- **Route loaders**: Prefer `ensureQueryData` in TanStack Router loaders to pre-fetch on navigation.

```typescript
// ✅ GOOD
const { data: trees } = useQuery({
  queryKey: queryKeys.trees,
  queryFn: getAllTrees,
});

// ❌ BAD — hardcoded key
const { data: trees } = useQuery({
  queryKey: ['trees'],
  queryFn: getAllTrees,
});
```

---

## 3. TanStack Router Patterns

- **File-based routing**: Routes live in `src/routes/`. Follow the file naming convention (`__root.tsx`, `index.tsx`, `$param.tsx`).
- **`routeTree.gen.ts`**: Never edit this file — it is auto-generated by the Vite plugin.
- **Typed navigation**: Use `Link` and `useNavigate` with typed route params.
- **Route loaders**: Use the `loader` function in route files to pre-fetch data with TanStack Query.

---

## 4. Zustand Patterns

- **Single store**: All global client state lives in `src/store/app-store.ts`.
- **Persistence**: Uses `persist` middleware (key: `vata-app-storage`, stored in `localStorage`).
- **Actions**: Define actions as methods inside the store creator, not outside.
- **No derived state in store**: Compute derived values in components or selectors.
- **Selector pattern**: Always select minimal slices to avoid unnecessary re-renders.

```typescript
// ✅ GOOD — select only what you need
const currentTreeId = useAppStore((s) => s.currentTreeId);

// ❌ BAD — subscribes to the entire store
const store = useAppStore();
```

---

## 5. React Conventions

- **Components**: PascalCase filenames and function names.
- **Custom hooks**: `use` prefix, camelCase filename (e.g. `useTreeList.ts`).
- **Memoization**: Use `useMemo` and `useCallback` only when there is a measurable render cost; do not over-optimize.
- **Props**: Destructure props in the function signature. Use an explicit interface for non-trivial prop shapes.
- **No default exports for hooks**: Named exports only for hooks and utilities.
- **Default exports**: Only for route components (TanStack Router convention).

---

## 6. Naming Conventions

| Concept          | Convention             | Example                             |
| ---------------- | ---------------------- | ----------------------------------- |
| Component        | PascalCase             | `TreeCard.tsx`                      |
| Hook             | `use` + PascalCase     | `useTreeList.ts`                    |
| Manager          | PascalCase + `Manager` | `IndividualManager.ts`              |
| DB function      | camelCase verb         | `getAllIndividuals`, `createFamily` |
| Type / Interface | PascalCase             | `Tree`, `Individual`                |
| Store file       | kebab-case             | `app-store.ts`                      |
| Constant         | UPPER_SNAKE_CASE       | `TREE_COLUMNS`                      |
